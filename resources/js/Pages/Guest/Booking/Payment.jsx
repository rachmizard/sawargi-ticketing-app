import UserLayout from "@/Layouts/User";

export default function BookingPaymentPage({ auth, ziggy: { query } }) {
    return (
        <UserLayout auth={auth}>
            <h1>WKOWAKOAKWOAKWOW</h1>
        </UserLayout>
    );
}
